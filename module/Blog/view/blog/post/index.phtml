<?php
/** @var \Blog\Entity\Post $post */
$this->headTitle("Blog");
?>
<div class="row">
    <h1>Posts da SON</h1>

    <div class="col-md-10">
        <ul class="list-unstyled">
            <?php foreach ($posts as $post): ?>
                <li>
                    <div class="jumbotron">
                        <h2><?= $this->escapeHtml($post->getTitle()); ?></h2>
                        <p><?= substr($post->getContent(), 0, 20) ?></p>
                        <p>
                            <a href="<?= $this->url('site-post', ['action' => 'show', 'id' => $post->getId()])?>"
                               class="btn btn-primary btn-lg">
                                Leia Mais
                            </a>
                        </p>
                    </div>
                </li>
            <?php endforeach; ?>

        </ul>
    </div>

</div>
